# coach.ai backend
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
sqlalchemy>=2.0      # 持久化层；默认 SQLite，无需额外驱动

# 切到 PostgreSQL 时启用：
# psycopg[binary]>=3.1
# alembic>=1.13       # 生产迁移（当前用 create_all）

# 开发/测试
pytest>=8.0
pytest-cov>=4.0
httpx>=0.27          # FastAPI TestClient 依赖

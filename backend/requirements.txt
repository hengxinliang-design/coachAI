# coach.ai backend — Phase 1 核心引擎
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
sqlalchemy>=2.0
# Phase 4 接入数据库时启用：
# psycopg[binary]>=3.1
# alembic>=1.13

# 开发/测试
pytest>=8.0
httpx>=0.27          # FastAPI TestClient 依赖
